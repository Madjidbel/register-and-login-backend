info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/users/login
  body:
    type: json
    data: |-
      {
        "email":"madjid@gmail.com",
        "password":"123123123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​