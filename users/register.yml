info:
  name: register
  type: http

http:
  method: POST
  url: http://localhost:5000/api/users/register
  body:
    type: json
    data: |-
      {
        "email":"mada@gmail.com",
        "password":"qsqsqsqs"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
