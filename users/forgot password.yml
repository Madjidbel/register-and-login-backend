info:
  name: forgot password
  type: http

http:
  method: POST
  url: http://localhost:5000/api/users/forgot-password
  body:
    type: json
    data: |-
      {
        "email":"madjidma2005@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
