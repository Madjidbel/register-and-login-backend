info:
  name: aothorization
  type: http

http:
  method: GET
  url: http://localhost:5000/api/users/admin
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
